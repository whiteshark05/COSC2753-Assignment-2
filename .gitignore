# Ignore everything in data/ by default
data/*

# Un-ignore the directories so Git can traverse them
!data/raw/
!data/processed/

# Ignore contents of data/raw, but keep its .gitkeep
data/raw/*
!data/raw/.gitkeep

# Keep data/processed tracked (remove data/processed/* and its .gitkeep rule)

# System and editor files
__MACOSX/
.vscode/
/.venv

# Task 4 (visual search) -- local, regenerable or oversized artifacts.
# NOTE: git has no trailing-comment syntax; every comment here is on its own line.

# Step 16 writes a copy of the selected model here; the shipped one is in outputs/task4_models/
models/

# Task 4's own split artifacts; the notebook's preprocessing regenerates them in ~15s
data/processed_task4/

# 21 ablation checkpoints, ~900MB. The per-rung result JSONs and progress logs are tracked instead.
experiments/*.pt
experiments/out_*.log
experiments/*_stdout.log
experiments/stop_*.log
experiments/aborted_*
experiments/rung0_probe_curve.json
experiments/rung1_stopped_comparison.txt
__pycache__/

# Byte-identical duplicate of outputs/task4_models/task4_gallery_embeddings.npy
outputs/task4_deployment_embeddings.npy

# Local notebook backups
notebooks/*.ipynb.bak
notebooks/*.ipynb.executed_backup
notebooks/COSC2753_A2_Task4_UPLOAD_NOW.ipynb
